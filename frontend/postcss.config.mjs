/**
 * PostCSS config — runs Tailwind + autoprefixer over globals.css at build time.
 * You normally never edit this.
 */
const config = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};

export default config;
